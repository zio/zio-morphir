package zio.morphir.ir.value

import zio.morphir.testing.MorphirBaseSpec
import zio.test._

object ValueSpec extends MorphirBaseSpec {
  def spec = suite("ValueSpec")(
    suite("Unit")(
      test("foldLeft should work as expected"){
        ???
      }
    )
  )
}
